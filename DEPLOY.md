# 部署说明

这是一个纯静态站点：没有构建步骤，把仓库内容原样放到任意静态托管上就能用。

## GitHub Pages

1. 把本目录内容放到仓库根目录；
2. 仓库 **Settings → Pages**，Source 选 `main` / `/ (root)`；
3. 等一分钟左右，站点地址是 `https://<你的用户名>.github.io/<仓库名>/`。

注意 GitHub 的规则：**Pages 在公开仓库对免费账号开放；私有仓库要用 Pages 需要 Pro 及以上**。
如果你的仓库是私有的，要么升级，要么改用下面支持连接私有仓库的托管服务。

所有资源都是相对路径引用，放在 `用户名.github.io/仓库名/` 这样的子路径下不会 404。

## 其他静态托管

Cloudflare Pages、Netlify、Vercel 等都可以直接连接 Git 仓库部署，
**构建命令留空**、输出目录填 `/` 即可。这几家都支持连接私有仓库，
免费额度足够个人使用。

## 更新之后：让浏览器丢掉旧文件

站点没有构建哈希，浏览器会分别缓存 `js/`、`styles/` 下的文件。
所以**推送新版本后第一次打开，可能只刷新了其中一部分文件**，出现"界面是新的、
版面却不对"的怪现象（曾经出现过卡片被压成细条）。

为此页面启动时会核对版本号：`styles/app.css` 里的 `--app-build` 与 `js/main.js` 里的
`BUILD`，两者不一致会在界面上提示强制刷新。

**改完界面相关文件后，记得把这两个数字一起加一。**

自己遇到版面错乱时，先按 `⌘/Ctrl + Shift + R` 强制刷新一次。

## 体积

仓库约 18MB，绝大部分是自带的 Noto 中日文字体（为了离线可用、导出不依赖 CDN），
远低于 GitHub 单文件 100MB 的限制。字体可以用 `tools/fetch-fonts.mjs` 重新获取。
